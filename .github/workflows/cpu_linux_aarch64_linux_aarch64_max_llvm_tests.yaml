# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CI - CPU Linux aarch64 -> Linux aarch64 tests. Max LLVM/sysroot

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  issues: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  # Don't cancel in-progress jobs for main branch
  cancel-in-progress: ${{ github.ref != 'main' }}

jobs:
  run-tests:
    name: "CPU Linux aarch64 -> Linux aarch64 tests. Max LLVM/sysroot. Runner ${{ matrix.job_info.runner }}"
    defaults:
      run:
        # Explicitly set the shell to bash
        shell: bash
    strategy:
      matrix:
        job_info: [
          {
            runner: "linux-arm64-c4a-16",
            container: "us-docker.pkg.dev/ml-oss-artifacts-published/ml-public-container/ml-build-arm64@sha256:1ee5aed2974d479c82125d1e5f91fb985a44bb5363bbff76048f265be95f0051",
          },
        ]
    # Use self-hosted runners. For a Linux runner, a corresponding container image used for the
    # workflow is required.
    # See go/ml-github-actions/getting_started/using_runners.md#supported-runner-scale-set.
    runs-on: ${{ matrix.job_info.runner }}
    container:
      image: ${{ matrix.job_info.container }}
    steps:
      - name: "Checkout repository"
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: "Clang 21, Glibc 2.31 | Testing..."
        run: |
          bazel test --cxxopt="-std=c++17" \
            --repo_env=LLVM_VERSION=21 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --color=yes \
            //cc/tests/cpu:all

      - name: "Bzlmod. Clang 22, Glibc 2.31, libc++ static linking | Clang header modules testing..."
        run: |
          bazel test --features=c++20 \
            --features=use_header_modules \
            --repo_env=LLVM_VERSION=21 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --config=bzlmod \
            --color=yes \
            --verbose_failures \
            //cc/tests/cpu/clang-header-module:basic_test

      - name: "Clang 21, Glibc 2.31 | C++20 modules testing..."
        run: |
          bazel test --features=c++20 --experimental_cpp_modules \
            --repo_env=LLVM_VERSION=21 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --color=yes \
            //cc/tests/cpu:utils_module_test

      - name: "Clang 22, Glibc 2.31, libc++ static linking | Testing..."
        run: |
          bazel test --cxxopt="-std=c++17" \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --color=yes \
            //cc/tests/cpu:all

      - name: "Bzlmod. Clang 22, Glibc 2.31 | Testing..."
        run: |
          bazel test --cxxopt="-std=c++17" --config=bzlmod \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --color=yes \
            //cc/tests/cpu:all

      - name: "Bzlmod. Clang 21, Glibc 2.31, libc++ static linking | Testing..."
        run: |
          bazel test --cxxopt="-std=c++17" --config=bzlmod \
            --repo_env=LLVM_VERSION=21 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --color=yes \
            //cc/tests/cpu:all

      - name: "Bzlmod. Clang 22, Glibc 2.31, libc++ static linking | Clang header modules testing..."
        run: |
          bazel test --features=c++20 \
            --features=use_header_modules \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --config=bzlmod \
            --color=yes \
            --verbose_failures \
            //cc/tests/cpu/clang-header-module:basic_test

      - name: "Bzlmod. Clang 22, Glibc 2.31, libc++ static linking | C++20 modules testing..."
        run: |
          bazel test --features=c++20 --experimental_cpp_modules \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --config=bzlmod \
            --color=yes \
            //cc/tests/cpu:utils_module_test