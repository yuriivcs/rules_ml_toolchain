# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CUDA 13 Linux x86_64 -> Linux x86_64 build

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  issues: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  # Don't cancel in-progress jobs for main branch
  cancel-in-progress: ${{ github.ref != 'main' }}

jobs:
  run-tests:
    name: "CUDA 13 Linux x86_64 -> Linux x86_64 build. Runner ${{ matrix.job_info.runner }}"
    defaults:
      run:
        # Explicitly set the shell to bash
        shell: bash
    strategy:
      matrix:
        job_info: [
          {
            runner: "linux-x86-n2-16",
            container: "us-docker.pkg.dev/ml-oss-artifacts-published/ml-public-container/ml-build:latest",
          },
        ]
    # Use self-hosted runners. For a Linux runner, a corresponding container image used for the
    # workflow is required.
    # See go/ml-github-actions/getting_started/using_runners.md#supported-runner-scale-set.
    runs-on: ${{ matrix.job_info.runner }}
    container:
      image: ${{ matrix.job_info.container }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

        # Clang 22 is minimum required version for CUDA13
      - name: "Clang 22, Glibc 2.31 | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Clang 22, Glibc 2.35 | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_35 \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Clang 22, Glibc 2.39 | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_39 \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Clang 22, Glibc 2.31, libc++ static linking | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Clang 22, Glibc 2.35, libc++ static linking | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_35 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Clang 22, Glibc 2.39, libc++ static linking | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_39 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "NVCC | Testing..."
        run: |
          bazel build --color=yes \
            --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "NVCC Static CUDA | Testing..."
        run: |
          bazel build --color=yes \
            --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "NVCC Static CUDNN CUDA | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --@rules_ml_toolchain//common:link_cudnn_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            --verbose_failures \
            //cc/tests/gpu:cudnn_test

      - name: "NVCC Static NVRTC CUDA | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --@rules_ml_toolchain//common:link_cuda_static_libs=true \
            --@rules_ml_toolchain//common:link_nvrtc_static_libs=true \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            --verbose_failures \
            //cc/tests/gpu:nvrtc_test

      - name: "Bzlmod. Clang 22, Glibc 2.31, libc++ static linking | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_clang \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --config=bzlmod \
            --repo_env=LLVM_VERSION=22 \
            --repo_env=SYSROOT_DIST=linux_glibc_2_31 \
            --@rules_ml_toolchain//common:stdlib=libc++ \
            --@rules_ml_toolchain//common:static_libcxx=true \
            --verbose_failures \
            //cc/tests/gpu:all

      - name: "Bzlmod. NVCC | Testing..."
        run: |
          bazel build --color=yes --config=build_cuda_with_nvcc \
            --config=cuda13 \
            --config=cuda_libraries_from_stubs \
            --config=bzlmod \
            --repo_env=HERMETIC_CUDNN_VERSION=9.12.0 \
            --verbose_failures \
            //cc/tests/gpu:all
